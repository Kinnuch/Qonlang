# 千语集插件示例

把这个文件夹整个复制到插件目录（设置 → 插件 → 打开插件目录），回来点「重新载入」。

- `plugin.json`：id、名字、入口文件。
- `index.js`：默认导出一个对象，`activate(qonlang)` 里注册扩展点；也可以直接用全局 `window.qonlang`。

插件跟千语集同权限跑，能调浏览器里的一切。装别人的插件前先看代码。

完整 API 见使用指南的「插件与 MCP」一页。
