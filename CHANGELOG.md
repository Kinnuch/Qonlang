# 更新日志 · Changelog

## 0.12.3 · 未发布

- **译文工作台**：语料、短语的译文旁多了一个「工作台」按钮。点开后左边整块换成工作台：中间写译文，软件按译文去词库里反查可能用到的词（汉字这类不分词的按字串找，拼音文字按词找；语料和短语里用过的排前面），以泡泡浮在下面；把要的词拖到上面一行（或点一下接在末尾），拖动可以调顺序，每个词还能挑用哪个屈折形。排好之后一键加进语料或短语，挑定的词直接确认挂好，不用再逐词核对
- **每日一句太长时怎么办，设置里三选一**（设置 → 应用（本机）→ 界面 → 「每日一句太长时」）：默认只从一行放得下的句子里挑；也可以照样挑长句、在词与词之间截断（按设置里的词间分隔，不会从一个词中间切开）；或者放不下就换行，卡片跟着长高。放不放得下按真实字体量出来的宽度算
- **有 gloss 缩写的维度处处写缩写**：维度取值填了 gloss 缩写的，词库的屈折形表、词条卡、录入页的屈折形列表、构形测试台都改写缩写（如 `SG.NOM`），鼠标停上去看全称；构形的槽位表去掉了单独的 gloss 一列，缩写写在槽位名下面，表不再被撑宽
- **释义输入框默认只给界面语言**：词条义项、例句译文、名称这些多语言输入框，原来按项目的「释义语言顺序」一种给一个（新建的项目默认中、英两个）；现在默认只给当前界面语言那一个，要填别的语言在设置 → 当前项目 → 语言与字体 → 「释义输入的语言」里加（如 `zh, en`）；已经填过内容的语言照旧显示
- **界面翻译插件**：新建翻译时可以替换开始页和「关于」里的「千语集 / Qonlang」字样，写成自己的文字（用这份翻译指定的字体显示）或换成一张图（小于 300 KB）；左边的 QL 图标不换。每条译文会记下翻译时的原文，原文后来改过的标上「原文已变动」（悬停看翻译时的原文），可以只看这些条；译文照样对的点「译文不用改」去掉标记。左边的分类不再露出 `csv`、`taxonomy` 这类键名：零散的几块并进所属模块（词类与维度算词库、手写板算文字、对话框和报错算通用），另有小游戏、字符、导入导出、插件与 MCP 四类；上面那排设置放不下时整块换到下一行，不再把「文字从右往左书写」这类标签挤成好几行
- **界面中文文案重写**：照着上一版定稿的 240 条的口吻，把其余文案逐条过了一遍，改写了 494 条：说完整的句子，操作写成「点击 / 勾选 / 输入」，界面上的名称加「」，提示讲清楚在哪、怎么做；上一版定稿的原文一条没动
- **语料列表里原文的标点和空格照原样画出来**：原来只画识别出的词，逗号、句号都丢了
- **阿拉伯语**：小游戏 Sage 等 6 处以拉丁字母打头的文案改成阿拉伯文打头，整段按从右往左排、靠右对齐（原来按第一个字母判断成从左往右，标题跑到了左边）
- 设置 → 插件里自带插件的卡片去掉浮起的阴影，上下间距对齐
- **修复：构形「微调」里写的音类定义、多合字母声明不生效**：原来这一步的规则逐行单独编译，定义只管它自己那一行；现在对这一步的每一条规则都生效
- **修复：音类里有重复成员时，替换按位置对错了位**：原来音类成员会自动去重，`{前}=m b d` 替换成 `{后}=w w dh` 这种两个音并成同一个音的写法，第二个 `w` 被去掉，b 就对到了 dh、后面全部错开一位；现在成员原样保留
- **修复：在项目里用「改」给短语的词换了词条，开始页的每日一句还是显示原来那个**：画廊里的短语原来每次都从原文重新自动分析，没读短语自己存下的分析；现在原文没动过就照存的来，跟短语页看到的一样
- **修复：每日一句右下角的落款（项目 · 语言）名字长的时候压在译文上**：原来正文只给落款固定留 96px；现在只有跟落款同一高度的最后一行让位，让多少按落款实际多宽算（落款最多占三分之一宽，多的省略）。项目名和语言名只差空格、竖线这些的（「北岸语 | Nordrin」与「北岸语 Nordrin」）也只写一个

## 0.12.2 · 2026-09-23

- **自带插件「界面翻译」**（设置 → 插件里勾上才出现，默认不开）：把软件里能翻的 2177 条文案按模块列出来逐条翻，左边是分类和进度，右边一行一条——左边原文、右边填译文；原文想对着哪种语言看自己挑，还能只看没译的、按键名 / 原文 / 译文搜。新建一份翻译时填：名字、**兜底语言**（没译到的条目按它显示）、**用拉丁字母还是自己的文字**（用自己的文字要指一款装好的字体，界面就用那套字）、**文字要不要从右往左写**。翻好的直接出现在语言选单里（设置和开始页左下角都有），一份翻译还能导出成文件发给别人、也能导入别人的；译文只存在这台电脑上，不进项目文件
- **阿拉伯语不再整页镜像**：只有文字本身从右往左排（每一段按它第一个有方向的字符定，夹在里面的拉丁字母和数字照旧），导航还在左边、检视器还在右边
- **修复：进短语页要卡近一秒**（79 条短语的项目）：原来一进页面就把每条短语整句分析一遍。现在列表只按写法判断每个词能不能点（同一个写法只算一次），整句分析留到真要用的时候（悬浮、改词）才算那一条；一屏先画 20 条、往下滚再续；认词要的索引挪到画完之后空下来再建。同一个项目实测进页面从 750 毫秒降到 10 毫秒上下，第一次进去也只有 170 毫秒
- 开始页左上角的字标与下面那句介绍**留出了空当**：原来按英文字标调的负边距，日文、韩文、俄文、阿拉伯文的字上沿更高，看着就贴到字标上了
- **每日一句卡片的渐变跟着主题走**：两个色标都是当前主色调转一点色相（同一张每次都一样），换皮肤整块跟着换，不再是六套固定配色
- 阿拉伯语的「短语」模块改叫 مكنز

## 0.12.1 · 2026-09-22

- **界面多了八种语言**：繁體中文、English、日本語、한국어、Français、Español、Русский、العربية——设置 → 界面语言（或开始页左下角的「语言」）里选。繁体由简体机器转换（台湾用词）并跟着简体走，其余是逐条翻译的；哪一条没译到会自动回落（繁体回落简体，其余回落英文），不会露出键名
- **阿拉伯语下整套界面从右往左排**：导航在右、检视器在左，各处的边距与对齐跟着翻（改用了 CSS 逻辑属性）
- 「主题」统一了叫法：开始页左下角原来叫「个性化」，现在跟模块一样叫「主题」；设置里原来也叫「主题」的那一项（跟随系统 / 浅色 / 深色）改叫「**明暗**」，不再跟主题模块重名
- **修好了短语页悬浮卡的「改」**：上一版这个功能跟语料页不是一套代码，点「改」会去改整个词而不是你正看着的那一段（标题里显示的也是整词），挑了一个词还不问一声就把原文改掉了。现在跟语料页走同一份代码——短语也能存下自己的分析，切分里点哪一块「改」就是哪一块（同一个词条在一个词里出现两次也分得清），只有勾了「**同时改原文**」（默认不勾，且只在改整词时出现）才会动原文和发音、文字写法，一次撤销整体退回；认不出来的那一段原来点不动，现在点了直接开「没有找到」让你指定
- 悬浮卡不再被路过的词抢走：鼠标从词挪到卡片上、或正在卡片里挑词时，排队中的另一张卡不会再顶掉当前这张
- **界面用语整体校过一遍**：239 条提示、标题、图文引导的说法按用户定稿改写，措辞更统一
- **图文引导重做**：步骤从 53 条扩到 98 条，十二个模块外加「插件与 MCP」各有一段；**引导会带着你走**——该讲哪个子页、哪张表、要不要打开检视器，它先切过去再讲，跳过或讲完把界面还原成你进来时的样子
- **修复：引导的高亮框圈错控件**（语素页第 3 步说「显示 / 录入」却圈住了「词条 / 统计」）。顺带查出开始页、构形、文档、语言几处也圈错或根本圈不到，一并修好；现在每个锚点都打在元素上（`data-tour`），不再靠按钮上的文字和位置去猜

## 0.12.0 · 2026-09-20

- **开始页重排**：起步模板挪到左边跟「新建项目」在一起，中间让给**最近项目**——一个项目一张卡片，右下角是这个项目自己的文字写的一个词（太长的那头淡出去，竖排的往下淡），鼠标扫过卡片里有一层跟着指针走的光；去掉了顶上的外链横幅
- **左下角多了「语言 / 个性化 / 设置」**：不打开项目也能换界面语言、换主题和皮肤预设、改应用设置（跟设置页「应用」那一档是同一份）
- **每日一句**：卡片左上角一个小图标、右上角写着「每日一句」（配图的词是「每日一词」），落款精确到语言（项目 · 语言），行数多的句子不再被卡片高度挤掉
- **语言树状图不再把分类节点算进对比**：两门语言的最近公共祖先只在语言里找（语系 / 语族 / 语支只是结构），一门是另一门的祖先时就显示那门祖先
- **短语页的悬浮卡也能「改」了**：跟语料页同一套——搜词条或语素点一下就把原文里这处换掉，发音、文字写法跟着重算，能撤销
- **文档里的 `[[…]]` 能链到各个模块**：除了词条，还能链语素、语言、音变规则集（可以指到某个阶段）、构形（可以指到某个槽位）、文字、例句、短语和别的文档页——写成 `[[语素:-lAr]]`、`[[构形:名词#单数.位格|名词 · 单数位格]]` 这样，中英文前缀都认，不写前缀照旧先当词条找；工具条上的「插入链接」选类型搜名字，点了直接跳过去并高亮；找不到的链接画成虚线并说明是哪一类没找到
- **字体库换了预览文字**：拉丁字体写 `The quick brown fox…`，中日韩字体再带四个笔画全的汉字，没有拉丁字形的文字（卢恩、符号）不写示例；有中文名的字体在英文名下面用灰字写出来
- **皮肤能导出和导入**：一套皮肤存成一个文件（配色、字体、可选带背景图），导入时先整窗试看，点「套用」才保存
- 小游戏改名：单词卡 Flashcards、Sage 拼词、猜词 Wordle、交叉填字 Crosswords
- 起步模板收进「新建项目」按钮里，点一下才展开；最近项目一排四张、最多两排，底下的小游戏按满两排留位子，不会因为项目多少上下跳
- **字体库里每一款字体的预览都随软件带着**：一打开就能看到它真正的样子，不用先下载。带的是每款字体只含预览那一行字的子集（一款几 KB，31 款合起来约 460 KB），要真正用还是点「下载」装整套
- 字体库的预览**用字体本身显示**（原来拿的是目录里写的名字，跟字体文件里的字族名对不上就白显示了）；卢恩、古突厥文这类没有拉丁字形的写同一句话的转写，符号字体摆一排符号
- 带边框的控件统一了三档：平时只有普通边框，鼠标停上去边框变成强调色，点开 / 选中时边框外再加一圈晕染
- 左侧模块图标和标志跟着皮肤变色
- 收起检视器时，左边的内容铺满整个宽度，不再吊在中间；设置里的两栏排版窄的时候自动变成一行一个
- 致谢与友链：kikomas 一律小写，友链那条换成「蓝地群岛 Wiki」；皮肤的导入 / 导出图标对调回来
- **修复：设置里插件与 MCP 那两个「使用指南」按钮的提示写成了键名 `nav.extending`**
- **修复：音变页右下角的「导出为文本」跟上方导出菜单里的是同一个**，去掉重复的那个

## 0.11.2 · 2026-09-20

- 语言树状图里的**历时阶段改挂在语言下方**（`PThsr › AThsr › OThsr › Thsr`，鼠标停上去看完整的）：原来一串小方块挂在右边，把左右的兄弟顶开老远，同一层的语言隔得没法看
- **树状图里分类节点不再跟原始语并排**：语系 / 语族 / 语支只是用来结构化的，不是真的语言——代表原始语现在排在分类节点下面、下一级语支挂在它下面（跟列表视图一致）
- **修复：词库列表里五角星和单词之间多出一个点**
- 致谢的测试和意见反馈里加上见坂静安

## 0.11.1 · 2026-09-20

- **词库可以收藏词了**：每行最左边一个五角星，平时看不见、鼠标扫过才浮出来，点一下就收藏（中间填成黄色）；页面右上角的星形按钮「只看收藏」一键筛出这些词，录入模式「单词」那一行也有同一个星星
- **记号**：录入时勾上「记号」，这个词在列表和词条卡里前面多一个符号（默认 `*`），用来标一批要紧的词；符号在设置 → 当前项目 → 基本信息 → 「记号符号」里改
- 致谢的赞助里加上 Kyiexitah，排在最前
- 首页画廊多一行文字写法时横条放高一点，文字不再顶着上边框；开始页中间那一栏的上下留白收紧，常见窗口高度下不再出现滚动条
- **所有导入的样例下面多了一块「测试台」**：自己敲一段输入（一行数据、几条规则、一行字形映射），按现在这套导入设置当场解析，结果跟样例一样显示——不用改文件反复试就知道某一列对不对、某个标记认不认得；改设置跟着重算，默认收起，敲的内容离开再回来还在
- **修复：构形里最底下那个槽位点「增加步骤」，菜单把面板顶开**、鼠标一挪菜单就没了够不着——菜单改成浮在面板上（下面放不下就翻到按钮上方），面板不再被撑高

## 0.11.0 · 2026-09-20

- **插件**：别人可以给千语集写插件了。插件放在「设置 → 插件」里那个目录，一个插件一个文件夹（`plugin.json` + 入口 js），跟软件同权限跑，能扩展五处：
  - **检视器面板**（可以限定只在某几个模块出现）与**左侧导航里的整页**
  - **命令**：出现在 Ctrl+K 命令面板里
  - **导入 / 导出格式**：出现在词库的导入、导出菜单里
  - **构形生成器**：槽位的生成器里多一种「插件」，由插件算出这一格的形式
  - 设置里能逐个开关、重新载入、看它注册了多少东西；`docs/plugin-example/` 是一个把五处都用一遍的示例插件
- **MCP**：设置 → MCP 打开后，千语集会在本机起一个带令牌的 MCP 服务，Claude、Cursor 这类客户端连上就能用九个工具：看项目概况、搜词库、查词条、跑音变、gloss 一句话、推导屈折形，以及加词条、改词条、加例句。写操作默认先弹确认（可以关掉），改动都进撤销栈；服务只监听 127.0.0.1，没有令牌一律 401

## 0.10.3 · 2026-09-19

- **首页画廊显示文字写法**：例句、短语卡片上面加一行按文字页的映射规则转出来的文字（用这套文字的字体），原文仍在下面、照旧能悬浮看词
- 小游戏挑项目那一屏四周留出内边距，卡片排得开一点；玩法里标题那一行固定在上面
- 填字的提示不再重复编号（题号本来就写在每条前面），导出的网页同样

## 0.10.2 · 2026-09-19

- **开始页多了「小游戏」**：起步模板下面一排四个玩法，都拿自己的词库出题——点一个先挑项目（只读来出题，不打开、不改动）再挑语言：
  - **单词卡**：正面词、背面义，可以反过来；没记住的会再来一轮
  - **Sage 拼词**：给几条释义和一堆字母（正好够拼出那几个词），点字母组词，点已选的撤回，也能单独看某一条的答案
  - **猜词**：六次机会，按这门语言的字母表判对错（`th` 这类多字母的字母算一格），字长 3～7 可选
  - **填字**：用词库排盘、提示是释义，可以「导出网页」（单文件 HTML，别人用浏览器就能填、能对答案、能打印成 PDF）或「导出图片」（空白盘面 + 提示，打印用）

- **音变里可以引用另一套音变的一段**：写一行 `-@ 规则集名 : 起始阶段 .. 终止阶段`（冒号后面不写就是整套），跑到这里就把那一段原样跑一遍——两门姊妹语共用的祖语段只维护一处，那边改了这边跟着变。引用来的阶段在这边也算数（测试台、构形的音变步骤、历史形式都挑得到），被引用的那套按它自己绑的语言解析；可以套引用（最多四层），转圈引用、名字或阶段写错都会在那一行上报错

- **修复：词库里表格、树形图会画出停用的槽位**：这两种看法是按维度铺格子的，停用的那一格原来画成空的「—」；现在整行、整列、整支都没有槽位就不画了
- **三种看法的顺序一致**：列表原来按形式存进去的先后排，现在跟表格、树形图一样按构形里的槽位顺序排，不在槽位里的排最后
- **加了维度之后不再残留「维度少一层」的旧槽位**（简洁模式）：原来加一个维度，写过写法的旧组合会单独留在列表末尾；现在列表就是新维度的完整排列组合，旧写法作为继承来源，在每一格上标「继承自 X」，点「改成自己的」就把它变成这一格自己的写法（复杂模式照旧各算各的）

## 0.10.1 · 2026-09-18

- **点词条卡里的义项就能换意思**：语料里悬浮一个词，卡片上的义项点一下，这个词就换成这个义项；先在卡片顶上点某一段（`delì-hi` 的 `delì`），点义项就只改那一段——一个词由几个语素组成时，也挑得出某一段是哪个意思
- **修复：义项多的词，后面的义项在候选里被挤掉**：义项候选原来排在所有切分候选后面，同形词、能拆成几段的词一多就超出候选条数上限（Cathine 的 `leka` 第三个义项就是这样没了，跟语域无关）；改成紧挨着它自己那条排，并且**不再截断**——义项是自己写的，几个就该给几个；真正会爆的是切分候选，那边本来就有自己的上限

## 0.10.0 · 2026-09-18

- **语系、语支整个包起来**：列表视图里分类节点画成一个框，祖语、下一级分组、语言都在框里；祖语排在语系和语支之间、跟分类节点的标题行对齐（分类节点只是个分类，它的代表原始语不算低一级），下一级的语族、语支往里缩一格；标题左边的箭头收起 / 展开，收起时写里面有几门语言。树状图右键一个节点收起 / 展开它下面的，节点下面写「+几」，图里的字跟界面别处一套字体
- **语料里改词能顺带改原文**：「改」的面板底下多一个勾选项（默认不勾），勾上后挑中的词条写法会替换语料原文里的这一处，同一句里别处的同一个词不动；文字和分析一起进一步撤销
- **一个词由几个语素组成时，文字那一行每段都写**：`naegō-moh` 这种带连字符的词、在语料里用「改」切分过的词，原来只写词干那一个字（词干没有对应文字时整个词都不显示）；现在每一段各自去词库、语素里找对应的字，找不到的那一段单独退回转写，不影响别的段
- **上锁的维度也能筛选**：上锁的维度点一下照样按它筛，底色深一点、带把小锁，跟没锁的分开；筛着的时候「清空筛选 / 筛出来的都启用 / 筛出来的都停用」另起一行，按钮样式跟别处一致
- **词条下方的例句高亮这个词**：例句里这个词（含屈折形、连写词里的那一段）标出来；设置里勾上「例句也显示文字行与出处」后，例句像语料页那样带上文字行和出处
- **搜出来的结果高亮**：顶栏搜索的命中处在页面上标黄，翻页、滚动都跟着
- **历史形式是否显示可以关**：设置 → 显示里的「词条卡里显示历史形式」，默认开
- 改历史形式时按回车就生效（原来只有点空白处才算）
- 流水线里套构形时，下拉里的基础变体跟着用户改过的名字走（原来一直写「通用」）
- **隔开写的词能跨词认出来**：词头里用 `…` 分成几段的词条（`ma…gò`），语料里写成 `ma 某某某 gò` 也认得出，每一段都挂上同一个词条、gloss 一样；文字那一行每段只写自己那一半
- **修复：词库表格滚得深了整张表发虚**：每行的高度对齐到整数个物理像素（原来量出来带小数，上面那截空白一累加，整张表落在半个像素上，字就糊了）；皮肤没有花纹时不再画那一层
- **手写板重新生成的字体保留字距、连字、hinting**：内嵌的是 TrueType 字体时，只把画过、改过的那几个字换进原字体，字形编号不动、新字接在最后，GPOS / GSUB / kern 和别的字的 hinting 原样留着（改过的那几个字自己的 hinting 对不上新轮廓，只能去掉；数字签名总会去掉；OTF、WOFF、TTC 或者没有内嵌字体时只能从零写一份，留不住）
- **画笔不再半路断掉**：拖的时候鼠标变成禁止符号、笔画断开（浏览器自己起了一次拖拽）已修；笔画画出画布也接着画
- **词素的异体形可以按维度取值挑**：每条异体形可以指定「只在这些维度取值下用」（比如与格那一格用 -lAn），构形推导时自动挑；几条都对得上时按对上的取值多少、有没有出现环境、写的先后排
- **语言页可以拖着排**：拖到分组上就挂进那个分组，拖到语言上就成为它的子语言，拖到卡片上下边缘插在两者之间，拖动时底下出现「放到最外层」；放不下去的描红，松手弹回原位，都能撤销
- **语言页树状图**：从语系往下画的谱系图，语言的历时阶段挂在旁边；滚轮缩放、拖动平移、适应窗口
- **两门语言对比**：Ctrl 点第二门语言（或卡片上的对比按钮），上方写出最近公共祖先，两条支线一路闪虚线，下面直接比这两门语言的数量、音位（共有 / 只有 A / 只有 B）、同源比例与对应词表
- **构形槽位的新算法**：槽位按「取了哪几个值」认，写过写法的槽位一直算数（换了维度也在，维度多一层的「时-体-人称」和「时-体」并存）；维度可以上锁，上锁后点维度只是筛选（固定下来的槽位底色深一点），还能按筛选批量启用 / 停用
- **简洁模式 / 复杂模式**（设置 → 当前项目 → 构形，默认简洁）：简洁模式下维度多的槽位没写法时自动接着维度少的那个往下变，复杂模式各算各的
- **影响发音改成生成器里的一项**：生成器下拉多了「组合（流水线）+ 影响发音」，不再是单独一个勾选框
- **重定位更好找**：鼠标放到槽位那一行，槽位名旁边出一个小箭头（双击槽位名照旧）
- **历史形式可以逐步改**：推得不对的那一步点一下就能改，改动记进词源的中间态，后面几步从改过的接着推，旁边的 ↺ 回到推导值；提示文字改成「不一致」
- **手改过的屈折形能重新按构形推**：表格、树形图里手改过的格子右上角一个 ↺，旁边还有「全部重推」连手改的一起重推
- **批量标签**：选中几条词条后「批量标签」里一起加标签、把某个标签加给其余几条、改名或者从它们身上去掉
- **词条卡分块更细**：词源、历史形式、词干、屈折形各是一块，皮肤页里可以分别排序、调字号；屈折形只列构形推出来的，词干另算一块
- **悬浮卡跟着词库的看法走**：词库里选了表格或树形图，悬浮卡、画廊里的卡片也照着显示
- 一个词条几个义项时，语料里每个义项都能挑（原来只有第一个义项）
- 词头、词干、屈折形里用斜线或逗号写的几个异写各自入索引，语料里写哪一个都认得出（`Degnes/Degnant`）
- 自定义关系种类：打完回车或移开焦点才算（原来打一个字母输入框就跳走）
- 页签分组的组名平时只留一个字，鼠标放上去才展开，省出上方的地方
- 示例工程 Aelith 用上：按维度取值挑的异体形（复数标记在与格前是 -lAn）、简洁模式下第三人称接着「极性-时」槽位往下变

- **语言页：语系 / 语族 / 语支节点**：「添加分组」建分类节点，语言和下一级节点挂在它下面，可以指一门代表原始语；选中节点看这一支的统计与对比——数量、音位对照、同源比例（按词源追到同一来源）、对应词表
- **语言内部的历时阶段**：一门语言可以分几个阶段（上古 → 中古 → 现代），各有名字和缩写；音变的阶段标记可以绑到「某语言 · 某阶段」；词条、语素能标所属阶段
- **合并为阶段**：已经拆成几门语言的历时链，在语言检视器里选从哪一门开始，一键并成一门语言的几个阶段——词条、语素、例句、短语挪过来记下阶段，音变绑定改到阶段，正字法、文字、方言按名字对上
- **词条的历史形式链**：显示模式里例句上方一行「PSkr … → PTsr … → Tsr …」——词源来源能沿着每个阶段都绑好了的音变推到这个词时出现，推出来跟词库写法不一样会提示
- **手写板大升级**：导入的字体里有的字直接在手写板打开（按轮廓编辑）；形状（矩形、椭圆、正多边形、直线）、选择挪动与缩放、节点编辑、宽高、水平 / 垂直翻转、反向、勾线、一键加粗、复制；防抖 0–10 可调（拉线式防抖，松手再平滑）；撤销重做、滚轮缩放
- **导出字体**：字形表「导出字体」→ TTF / WOFF（原字体的字加上手写、改过的字，手写的覆盖原来的），或「写回内嵌字体」直接换掉这套文字的内嵌字体（不保留原字体的字距、连字）
- **构形槽位键跟维度先后无关**：A.B 和 B.A 是同一格——换维度先后、删掉维度再加回来、维度顺序不同的构形互相继承，写好的写法都还在（旧文件打开时自动转换）
- **槽位继承**：流水线的起点可以选「继承」本构形或别的构形的另一格，从那一格推出来的形式接着加步骤（那一格手改过就用手改的）
- **影响发音**：槽位勾上后另写一条发音流水线（从这一格按正字法转的 IPA 或词条发音开始），推导时发音存在形式上，词条卡里跟在形式后面
- **双击槽位名重定位**：写错格子的写法挪到别的槽位，原来那格变回「无」
- 构形测试台的输入框有字时右边一个 ×，一下清空
- 发音一栏改叫「基于正字法的 IPA」（几套正字法时带上正字法名）
- 示例工程 Aelith 用上这些：「Ael 语系」节点与统计、Aelith 的古典 / 现代两个阶段与历史形式链、名词复数的格继承复数主格、位格影响发音
- **皮肤预设重做**：名字统一成「品名·颜色」，排成两排——亚夜花园·青（原默认）、林中幻想·绿（原竹林）、契耶西塔·紫（原西幻）、新增海岛诺连·靛、蓝地渐歌·蓝（原海蓝）；星月夜·黑（原墨夜，改成满屏细碎星点的蓝黑夜空）、山海经·褐（原中国古代，强调色改褐色）、新增梅珠撒·红（红配白）、瓦其夫·黄（深藏青底、金黄强调、红色警示）、雪利兹·橙（柔和的橙白）。原来选过的预设照旧
- **换皮肤像墨水晕开**：点预设后新皮肤从点的地方几团软边圆一起洇开（系统要求减少动效时直接换）；强调色很亮的皮肤，主按钮上的字换成深色
- **皮肤支持自定义背景图**：铺满 / 完整显示 / 平铺 / 居中 / 拉伸，可调对齐、不透明度、缩放、模糊；图盖在整个窗口上、不挡点击，浅色正片叠底、深色滤色，字不会被盖灰；换预设、恢复默认都不动它
- **音变、构形的页签可以分组**：像浏览器的标签页分组，点组名收起 / 展开，右键页签移到别的组、右键组名改名换色解散，拖到别组的页签上顺便进那一组。默认分法：音变按共时（阶段都在同一门语言里、构形里跑的）/ 历时（阶段跨两门以上语言），构形按绑定的词类
- **音变阶段可以收起**：每个阶段标记前一个小三角，收起时显示有几行；两个阶段以上有「阶段全部收起 / 展开」
- **构形流水线复制粘贴**：每个槽位能复制、粘贴写法；变体栏「复制这套」「粘贴到这套」整套搬（槽位对得上的换掉，对不上的跳过），也写进系统剪贴板，能贴到另一个项目
- **示例工程能改不能存**：打开后随便改，顶栏标「示例」、保存按钮换成「复制一份」，Ctrl+S 只提示、关掉也不问；示例卡片上也能直接「复制一份」存成自己的项目
- **设置页分门别类**：应用（本机）分界面、保存与启动、更新、显示，当前项目分基本信息、语言与字体、分词与 gloss，再是数据和关于，每类一张卡片
- **开始页默认摊开更新日志**
- 发音两边符号的选项改叫「音位标注 /…/」「音值标注 […]」
- 开始页画廊里的例句、短语保留原文的句号逗号；短语里的词悬浮时跟语料一样认（按边界切开、带词缀的也认得出）
- 修复：只有一个附着词的词（`jehr` 这类）悬浮认得出，自动 gloss 却是空的
- 修复：皮肤页词条卡字号滑块（还有字形手写、配图裁剪的滑块）不跟主题变色、悬停时变成刺眼的亮青
- 导航栏左上角的图标跟顶栏的搜索框对齐；开始页左下的语言选择、版本号跟右边底部那排按钮对齐

## 0.9.3 · 2026-09-17

- **收不到更新推送的问题**：检查新版本改成先看 GitHub 发布页「最新版本」跳到哪个版本，不再每次都占 GitHub 接口的匿名额度（不登录时每个网络出口每小时只能问 60 次，几个人共用一个代理或校园网很快用完，之后就一直静默地「没有更新」）；发现新版本才问一次接口拿校验值和更新说明，接口被限流或连不上时按安装包的命名规则直接找本机的安装包（Windows 按 latest.yml 的 sha512 校验），并等到 GitHub 说的恢复时间再问接口
- **默认每 20 分钟检查一次**（原来 5 分钟，没改过的升级时自动改成 20）
- **设置 →「关于」重新排**：整套标志放大；写上当前版本和 Release 上的最新版本；新增「立即检查更新」按钮，结果写在旁边——已是最新、有新版本，或者检查失败的原因（连不上 GitHub / 查询次数暂时用完）
- 导航栏左上角的 Q 图标按视觉重心对齐下面的图标（字形笔画偏左，原来看着偏左 2 像素）
- 致谢「logo 设计」加上 sgiofh；logo 图与下面的像素画左边、名字都对齐
- 修复：安装包找不到或起不来时主进程弹「A JavaScript error occurred」崩溃框，现在在更新提示里显示失败原因


- **语料分词大改，黏着语连写的词也认得出**：一个词可以是「前缀 + 一个或几个词干 + 后缀」，词缀叠几层、两个词连写再接词缀（`yvplihemeliaxetes` → yvpli-hemelia-xete-s）都能切开；几种切法按块数、单字母、异体形的出现环境、本句译文和已确认的语料排序。按边界符切开的每一段也照这样切，段里可以只有词缀
- **跟已确认的语料学切分**：同一段文字在别处确认成了哪个词条或语素、连着几段确认成了怎么切（`don` 确认过是 `do-n`）、哪个语素后面常接哪个，下次都照着排，语料确认得越多越准。瑟乌丝林语测试集上第一候选的词条正确率从 73% 提到 86%，切分正确率从 74% 提到 78%
- **可疑的切法标成猜测**：拼了两个以上词干（除非这几个词连着确认过）、或用到还没确认过的单字母语素的结果，确认之前标黄、不算认出
- **构形推出的屈折形也用来分词**：词条没点过「推导」时，构形里只加词缀、重叠的槽位现推出来找，gloss 带上槽位缩写
- **「没有找到」的搜索框能挑切分**：输入的写法能切开时，先列出几种切法，挑一个就把这个词（或没找到的那一段）换成这几段
- **长释义的 gloss 截短**：自动 gloss 取释义的第一小段，截到冒号、括号前（「动词头：式 × 体……」只取「动词头」）
- 分词索引按项目缓存，整批重新分析时不再每句重建
- **新标志**：应用图标换成新设计的 Q 字图标（绿底黄字，Windows、macOS、Linux 安装包与窗口都换了，macOS 版按系统规范留边）；左侧导航栏顶上、开始页的品牌区、设置页「关于」用上新图标与字标，深色主题下标志换成品牌黄；网页版有了标签页图标。整套标志与色值放在仓库的 `docs/brand/`
- **致谢加上「logo 设计」**：新标志的设计者 kikomas，旁边放了 logo 图
- 修复：英文界面开始页的「Qonlang」字标比中文的略靠上

## 0.9.1 · 2026-09-16

- **大词库不再卡**：词库表格只画看得见的几十行，滚到哪画到哪，滚动位置照旧记着（原来一进来就画 150 行、滚得越深回来画得越多）；几千条、带文字列的项目，切回词库从近一秒（滚深之后两秒多）降到一百毫秒左右；按列排序时每行只算一次排序键，按文字列排序不再卡死
- **文字转写快了几十倍**：编好的转写规则按文字记下，项目改了才重新比对；转过的词直接用；目标是固定一串字的规则，词里没有这串字就不跑（音变、正字法也受益）；「有没有手写字形」每套文字只数一次；音节拼合用的表不再每个词重建一次
- **改东西后不再卡一下**：撤销记录直接序列化，不再先把整份项目逐个值克隆一遍，并且等主线程空下来再记；崩溃恢复快照直接用撤销记录那份；存盘、自动保存的排版和加密挪到后台线程
- **意音文字按字号写**：文字的「转写来源」选检视器模块（比如把字号 aa01 填在词条的「文字」栏）时，语料、短语、文字页预览和导出里的文字行逐词找到词条，用词条那一栏写，找不到的词照原文；词库、例句里手填的文字写法里写字号也换成字；字形「转写」的说明改成「转写来源那一栏里的写法」
- 修复：词库录入里文字写法框的灰字提示按单词转写，转写来源选了别的栏时显示的是拼写
- 修复：文字页预览里的词和例句没按转写来源、手填的写法写
- **撇号这类符号算字母**：词库里有词以撇号开头或结尾时（阿拉伯语转写的 `'llhi`），语料分词不再把它当标点剥掉；「设置 → 项目 → 算作字母的符号」里还能自己写几个
- **新建构形变体先照搬当前这一套**：新变体直接带着正在看的那一套的写法，改几处就行（继承来的仍然继承）
- **同一个构形的两个变体可以用在一个词条上**：词库录入的「加一个构形」能再挑同一个构形的别的变体，两套槽位分开显示（组名带变体名）、形式各存各的，不再互相覆盖
- **检视器里的灰字提示能看全**：框里的提示放进悬浮提示，鼠标停上去显示全
- **屈折形能换成表格或树形图**：词库检视器里屈折形那一块，标题右边三选一——列表（原来的样子）、表格（第一个维度作行、第二个作列，第三个维度起每种取值一张表）、树形图（按维度分叉，分叉点能收起）；录入模式里表格和树的格子就是输入框，可以直接在表里填，显示模式只看。两种模式共用一个选择
- **屈折形整块能收起**：显示模式、录入模式的屈折形标题旁都有三角，收起后写着「几格、填了几个」；两种模式各记各的
- **默认窗口跟着屏幕大小**：头一次启动时按屏幕的比例定窗口大小——2K 屏（2560 × 1440）上是 1853 × 920，别的屏幕同比缩放（125% 缩放的 2K 屏上是 1482 × 736 个逻辑像素，看上去占的地方一样大），最小 900 × 600，屏幕放不下就占满工作区；已经记下的窗口大小照旧不动
- 示例工程 Tsahun：加了按字号写的意音文字「Tsahun 刻符」（检视器模块「字号」，语料 `ngo21 kwe51 ta33 sip51` 写成 ⼰ ⼝ ta33 ⿂）

## 0.9.0 · 2026-09-15

- **重音规则**（音变、正字法这些写规则的地方）：一行 `ˈ = 条目 , 条目 …`，词走到这一行时标上重音，之后的规则都带着它（音和音之间隔着重音记号照样匹配），直到下一条重音规则重新标。条目写第几个音节（`1`、`-2` 倒数第二）或从前、从后找第一个合条件的（`*`、`-*`），可以加上音节核里要有什么（`{长元音}`）、环境（`/ _CC`，下一个音节的起首也算）、词有几个音节（`(2)`、`(4+)`），从左往右第一条对上的算；末尾写 `| · -1` 按中点把复合词分开各算、主重音落在最后一个词；`ˌ = …` 标次重音。规则里写 `ˈ` 指重读音节（`V > ə / σ(C)(C)_ - ˈ(C)(C)_` 弱化非重读元音）。测试台的每个阶段、每一列和轨迹都显示重音记号；规则列表里「新增重音规则」逐条编辑
- **音节边界 σ**：规则里写 `σ` 指音节边界，词首词尾、`·` `-` 这类分隔符两边也算——`d > t / _σ` 音节末清化、`V > Vː / _σ` 开音节里的元音延长。音节按这门语言的音节设置划，多合字母、多字母的音位算一个音；希腊字母 σ 本身写 `\σ`
- **特征**：一行 `[+送气] = ph th kh` 定义一个特征，从这一行起规则里可以写 `[+送气]`、`[-送气]`（`a > e / [+送气]_#`），几个写在一个方括号里取交集（`[+浊 -鼻]`），两边都列了成员时目标和替换按位置对应（`[+送气] > [-送气]`）。跟音类不同，特征按行生效，后面可以重新定义。规则列表里「新增特征」
- **自定义重音规则**（音系 → 音节与韵律）：重音位置选「自定义重音规则」，照上面的写法逐条编辑；测试和词库样例按它标重音，音标里已经带重音记号的（正字法规则标的、手填的）照记号来
- **对重音影响**（词库、语素的检视器）：勾上后多出「传递词性」「传递特殊重音」两个勾选项，特殊重音选第几个音节、倒数第几个或不重读，语素还能选「算作」哪个词类。重音规则跟着多了三种写法：`<动词>` 只对某些词类（`<代词|小品词>` 几个、`<!动词>` 不是这些），位置写 `0` 不重读，单独一条 `@` 先按词条自己标的特殊重音——`ˈ = @ , <代词|小品词> 0 , 1`。自动标音、整库演化、构形里的音变步骤、音系页的样例都带上；音变测试台里敲的词对上勾了的词条、语素时也带上；构形加词缀时特殊重音跟着挪，重读的词缀把重音拿到自己身上。音系「自定义重音规则」的表单里有「先看词条标的特殊重音」和每条的词类；CSV 导入导出多了「对重音影响」一列
- **音节与韵律按正字法切音节**：测试输入可以选「按拼写」或「按 IPA」，按拼写时先用基准正字法转成音标，th 这类写法算一个音（`ce·theurian` 不再切成 cet.heurian）；分隔符两边各自划音节，显示时照原样留着；词库样例用现转的读音，改了正字法规则马上看得到；没写转音标规则的正字法按各音位的写法换
- **构形按拼写数音**：中缀插在第几个音后面、模板取第几个辅音、重叠取头几个音时，主正字法里 th、eu 这类写法算一个音
- **可插入的音类、语素超过五行就在那一块里滚动**：规则编辑表单里的「点击插入」、音类与多合字母、语素检视器里的「用到它的词条」、语料统计里没认出的词、词库录入的方言
- **规则语法文档**加了「音节边界」「重音规则」「特征」三节，常见写法补了二十来条，文档里的例子都有测试跟着
- **手写字形**（文字 → 字形表）：「手写字形」或字形检视器里的「手写这个字」打开手写板。画板上有上伸线、大写高、x 高、基线、下伸线；画笔一笔一笔画，框选框住笔画后按住选框拖着挪、Delete 删，橡皮点到哪笔删哪笔，右边竖线下的圆点拖字宽。保存后软件把画的字做成字体挂在这套文字最前面，字形表、词库、语料、转写里都照样显示；字符空着时自动分一个私用区码位
- **构形推出来的词生成到词库**：词库录入里推出来的每个屈折形、构形测试台的每个结果旁边多了「生成词条」，弹出新词条表单，词源（派生 ← 原来的词条，备注写构形与槽位）和关系已经填好，词头、词类、构形、释义、标签可以现填；也能直接「只生成形式、词源和关系」
- **构形套构形**：流水线加了「构形」一步，把到这一步为止的形式当成词干，套另一个构形的某个槽位（比如动名词再按名词变格），最多套 4 层
- **测试台分对比、自由两种**：对比是原来的挑一个词跟词库比；自由随便写一个形式，只看它经过这个构形变成什么，结果可以生成词条，也能「接着构形」拿去套别的构形
- **一个词条几个构形**：词库录入的构形下面「加一个构形」（一个词既是名词又是动词），推出来的形式按构形分组；跟前面重名的槽位，名字前面加构形名
- **构形的槽位逐条收起**：可视化里每个槽位最前面有个三角，收起后只剩一行（槽位名、gloss，后面小字写这一格怎么构成，点小字展开）；「槽位」标题右边有「全部收起 / 全部展开」。收起状态记在本机，从表格、树形图点一格跳回来时那一格自动展开
- **词库缺释义标红**：没写释义的词条标红；列表底部右边多了状态栏，数出缺释义（红）、词头重复（黄）各有几条，鼠标放上去列出是哪些，点一下跳过去
- **纯欣赏副本加密**：导出的纯欣赏副本整个加密压缩，拿记事本打开只是一串乱码；打开纯欣赏项目时，另存文本、导出（CSV、词典、文件夹、PDF……）和复制结果都不做，正式版里开发者工具也打不开；另存为照样存成加密的副本
- **页面出错不再整页崩**：底部挂一条红色的条，写明哪一页出的错、报错信息，要手动关掉；项目自动退回到上一次操作之前，这一页重新画出来，可以接着用
- **检视器宽度跟着窗口走**：主区留够语言、设置这类页面的内容宽，剩下的都给检视器（默认窗口下两边正好对齐）；拖了分隔条就按拖的宽，双击分隔条回到跟着窗口走；旧版本记下的宽度升级时先改回跟着窗口走一次。开始页右侧面板不变
- **词库表格铺满**：列宽按表格区域铺满，右边不再空一大片；加了列先按比例匀出已有各列的宽，实在放不下才横向滚动；拖一列的宽时，右边紧挨的那列反着让
- **搜索语法维度与方言**：词库、语素的顶栏搜索可以写 `feature=取值`，或者直接拿维度名当字段（`性=阴`）；词库还能写 `dialect=方言`；词库多了「方言」列，表头漏斗可以筛
- **右键菜单加「另存为」**
- **更新**：还没真正开始下载（增量更新在比对）、或者下完在校验时显示「校验中」，进度条等真正开始下载才出；装之前存项目——存过盘的直接存，没存过的弹另存为，取消了先问「这会导致更新进度归零，继续吗？」
- 修复：深色模式下悬浮词卡里没选中的拆解框，字太暗看不清
- 修复：开始页画廊里悬浮例句的词，有的整个认不出来、有的拆开后点了没反应（现在跟语料页用同一套认词）
- 修复：词库检视器的例句挂上了不相干的句子（词干槽里只填了一个字母时，所有单独有这个字母的句子、文档行都算进来）
- 修复：词库录入里先后加的两个词干互换名字后，显示模式里的顺序没跟着变
- 修复：在一个构形的变体上编辑时切到别的构形页签，整页生成器都变成「无」、流水线全空
- 修复：音变规则的一行里几次用到没定义的音类，整页崩掉；正字法、阶段、预设、标签重名时同类的崩溃一并处理
- 修复：文字页「自动映射」里的字形没用这套文字的字体，显示成方框
- 修复：源代码视图里选中文字时，选中的底色把字盖住了
- 修复：音系检视器里正字法测试按阶段分列的表，表头和各格改成居中
- 修复：正字法转音标时，多合字母里本来就是音标的内部符号又被换回写法（th → θ 转完变回 th），音节与韵律、配列检查、字形分类跟着切错
- 修复：规则列表里显示、编辑用到多合字母的规则时写成了内部符号（写的 `th > θ` 显示成 `θ → θ`，改一下就把 θ 写回规则里）
- 修复：规则环境里写 `[a-z]` 这类带连字符的临时音类，连字符被当成了排除
- 修复：深色模式下词库底栏的「缺释义」、构形流水线里的「词干」、短语分类的计数字不够清楚
- 示例工程：Aelith 新增「书面语 → 口语」规则集，演示特征、重音规则和 σ；罗马化转音标的最后一行是重音规则，代词、小品词勾了「对重音影响」不重读，telikaso 标了特殊重音读 teliˈkaso，附着词 =mU 不重读，前缀 be- 算作形容词；姊妹语 Merun 用自定义重音规则，hara 标了特殊重音；vesa 故意没写释义，词库底栏点得到。Tsahun 的音位填了罗马化写法，强调重叠 tsing55 得 tsitsing55
- 测试夹具（瑟乌丝林语）：正字法转音标按语法书补上重音规则（标了半长元音的音节、双音节词、倒数第二音节的长短、复合词各词分开算），修掉 ch 跟合字 x 分不开、双元音里的 a 被换两遍、专名开头的大写字母没转的问题；音节与韵律改用自定义重音规则

## 0.8.4 · 2026-09-14

- **英文版使用指南**：指南有了完整的英文版（kinnuch.github.io/cerf/qonlang/en/，章节、锚点跟中文版一一对应，每页顶上能互相切换）；界面是 English 时，各页标题旁的「User guide」、图文引导结束的弹窗、开始页的按钮、帮助菜单和右键菜单都打开英文版
- **在语言页点一门语言就换成当前语言**：树里（或检视器的谱系里）点哪门语言，顶栏右上角的「当前语言」跟着换成它；反过来在顶栏换了当前语言，树里也选中它
- **深色模式柔和一些**：强调色压暗、降饱和，主按钮、选中的描边、页签、徽章、提示条不再是扎眼的亮青绿，主按钮上的白字也更清楚；弱文字稍微提亮；音系、文字页标题旁用语言颜色填底的徽章蒙暗一层；开始页画廊的渐变少混一点颜色；规则高亮里替换部分的颜色、音类圆框里的音类名换成不刺眼的颜色
- **窗口默认 1700 × 900**：开始页打开右侧面板时英文界面底部那排按钮也排得下一行，有「恢复未保存的工作」提示条又展开新建项目的表单时也不用滚动；旧版本记下的比这小的窗口先放大一次（屏幕放不下就占满工作区）
- 致谢的测试名单加上 Ethan Delanche
- 修复：开始页「更新日志」面板下面显示不全（面板里又套了一层固定高度的滚动框）
- 修复：英文界面、或者有恢复提示条时，开始页的画廊被压扁
- 修复：英文界面开始页底部那排按钮，窗口拉多宽都有一个挤到第二行
- 修复：深色模式下，音变测试台隔行的底色只画了半行（第一列没跟着变），图文引导气泡里白底按钮上的字看不清

## 0.8.3 · 2026-09-14

- **关系图能拖、能展开收起**（词库 → 关系图）：按住空白处拖动画布，Ctrl + 滚轮缩放，左上角有放大、缩小、回到中心；右键节点「展开以它为中心的节点」把它自己的来源、派生、同源……再画一圈（已经在图上的词不重复画，只连线），「收起以它为中心的节点」收回去，中心词也能收起再展开；空白处右键「回到中心」「全部收起」。语素节点展开能看到用到它的词（包括自定义来源里写到它的），对不上词条的来源成分展开也能看到写到它的词
- **关系图的对比**：跟中心词同一个词根来源的词有两个以上时，画布右上角出现「对比」。上面挑词根（几组）和要比的词（最多 6 个），表里并排比：发音；意思（几个词共有的意思成分标底色）；从词根到这个词的来源链；一路加进来的成分（前缀、后缀、复合的另一半，只有部分词有的高亮）；按音变规则集重新推一遍的音变（每一步的形式、用上的规则，推出来跟词库对不上的标出来）；词类、语域、标签、方言的差别。同一套规则推出来的几个词再列「音变差异」，只看部分词经历了的音变；不同语言的同源词再列「语音对应」，词根里的每个音在各个词里变成了什么。词根按词源一路往上找，前缀、后缀不算词根，自定义来源按语素边界拆开的成分也算
- **悬浮词卡上的「改」**：词认错了（悬浮出来是别的词）就点卡片底部的「改」，换成搜索框挑正确的词条或语素，挑中写进这个词的分析并确认；卡片正显示切分里的某一段时只改那一段；几个候选都不对时点「都不对，自己找」。开始页画廊里点「改」先打开那个项目、跳到语料里那一句，直接在那个词上打开搜索框
- **刷新当前页**：顶栏重做右边的按钮（或按 F5），导入、新增之后哪里没跟上时点一下，这一页整个重新加载；筛选、选中这些都还在
- **皮肤页预览词条卡**：右边的预览里多了一张真的词条卡（用项目里内容最全的一个词），调各块字号、拖顺序马上看得到；鼠标放在左边哪一行，预览里描出哪一块
- **macOS 也能自动更新**：点「下载并安装」下载对应芯片的 zip，按 GitHub 给的 sha256 校验，解开后换掉旧的千语集再重新打开（Apple 芯片上跑的是 Intel 版时顺手换成原生版）；千语集放在换不了的地方（直接从 dmg 或下载目录打开）时退回打开 dmg，提示框写明接下来拖进「应用程序」。从 0.8.3 装好之后的下一次更新起生效
- 示例工程 Aelith：加了姊妹语 Merun（规则集「Proto → Merun」：词首 k 弱化成 h、辅音后的词尾 i 脱落，kasu 在这里是「帐篷」），kaso、teli 的关系图点「对比」能看两边的音变、语音对应与意思
- 修复：从词库检视器的例句（或命令面板、别的页面）跳到语料、语素、短语、词库时，列表不滚到那一条、也不闪——目标不在先画出来的那一批里时还没画出来，进页面时恢复上次滚动位置又会把滚动条拉回去；现在先画到那一条，滚到了再闪
- 修复：开始页画廊里悬浮出来的词卡，文字行有时是方框（那个项目没打开，文字的内嵌字体没注册）
- 修复：Mac 上点「下载并安装」只打开 dmg，提示框一直停在「正在安装」；提示新版本时 Release 上本机的安装包还没传完的，之后再查到就换成能直接装的；检查新版本带上 ETag，没变化时不占 GitHub 接口的额度，被限流时沿用上一次查到的

## 0.8.2 · 2026-09-14

- **开始页的画廊**：「万千世界……」横幅下面多了一条画廊，从最近打开的项目（最多 3 个）里抽语料例句、短语和带配图的词，一张一张换着看。例句、短语用渐变色做底，带配图的词拿那张图裁满横条、模糊了当底；鼠标移上去左右两边才出现半透明箭头，点了滑到下一张；顺序按当天的日期打乱（同一天打开第一张不变），同一个项目里原文一样的只出一张。例句、短语里的词可以悬浮看词卡，卡片上的「在词库中查看」先打开那个项目再跳过去；最近打开的项目里什么都抽不到时整条不显示
- **短语、语料拖着换顺序**：短语页、语料页的卡片按住拖到另一张上，松手就换到那个位置
- **短语的发音可以重新按规则生成**：检视器里每套正字法的发音后面加了「!」（不规则）勾选框，手改发音会自动勾上；勾掉就马上按规则重算。「按规则标音」（和改完原文时的自动标音）照旧不动勾着的，但提示条会说有几个没动，点提示条上的「也按规则重算」一并重算
- 修复：悬浮词卡里的环缀只显示前一半，现在前后两半都显示（`前半…后半`）；词卡的候选列表、拆解那一行、语素的词源里同样

## 0.8.1 · 2026-09-13

- **词条卡每一块单独调字号**（皮肤 → 词条卡）：去掉了整体的倍数条，每一块（词头与发音、义项、标签、词源、词干与屈折形、关系、派生词、备注）一行一个字号滑块，直接是像素；0.8.0 调过的倍数打开时自动换算成字号
- **词类与维度逐条收起**：每个词类、维度、检视器模块前面有个三角，收起后只剩一行（名字、缩写，后面小字写着绑的构形、词干槽、取值、用于哪些词类）；每块标题旁有「全部收起 / 全部展开」，收起状态记在本机
- **一个词类绑几个构形**（构形 → 检视器 → 绑定词类）：比如「动词」同时绑变位法一、变位法二；先绑上的是默认，别的可以「设为默认」；词条录入时构形下拉把这个词类的构形单列一组，挑了哪个就按哪个推导；构形测试台先列出正在用这个构形的词
- **按条件换字母**：前缀、后缀、中缀、环缀和微调里写 `{阴:g|k}`——词条（或这一格）是「阴」时用 g，否则用 k，其余部分不变；条件写维度取值的名字或缩写，`性=阴` 指定维度，逗号是任一、`+` 是都要；流水线里的词缀旁边小字列出每种挑法，推导轨迹写明挑了哪一支；语料反推词缀、统计里的语素引用按每种挑法都算
- **造词参考词库**（音系 → 配列 → 造词）：照着这门语言词库里已有的词学——词首词尾常见什么音、几个音节的词多、音和音怎么搭配——先撒一大把候选，按像不像这门语言排序，再挑出彼此不太像的一批，尽量不跟已有词只差一个音；配列设置仍是硬规定，每个结果都重新划音节查一遍配列；悬停看「像：……」；词库少于 8 个词或关掉「参考词库」时只按配列造
- **增量更新**（Windows 安装版）：提示有新版本时点「下载并安装」，只下载安装包里跟上一版不一样的块（实测一次小改动：整包 95.7 MB，只下 1.6 MB），拼好后按 Release 上 latest.yml 的 sha512 整个校验；对不上、Release 上缺更新信息、或者中间出任何错，都自动改成整包下载（整包同样校验）——装上去的一定跟 Release 上的安装包一模一样。安装照旧静默、沿用上次的目录并自动重开。
  从装着 0.8.1 正式版的下一次更新起生效（更早的版本、免安装版、本地打的包、macOS 仍整包下载）；每次下载怎么走的记在数据目录的 update.log。Release 上多挂 latest.yml 和安装包的 .blockmap 两个文件
- **构形的表格与树形图**（构形 → 槽位标题旁）：跟音变页一样可以切换看法——「可视化」是原来逐格编辑的样子；「表格」把第一个维度排成行、第二个维度排成列，多出来的维度每种取值各画一张表；「树形图」按维度一层层分叉，分叉点可以收起。格子里是测试台上那个词的推导形式，小字是这一格的写法（每一步的词缀、跑哪套音变），推导跟已录入形式对不上的标红；点任意一格回到可视化并滚到那一格
- **拖着换顺序**：构形的维度、流水线里的每一步、词类的组成与适用词类、检视器模块的适用语言、音位表里的音位、正字法、音变规则里的音类与多合字母、标签（词条、义项、语料、短语……），这些小圆框都可以按住拖到另一个上面，松手就换到那个位置；构形、音变、文字自己新建的页签也能拖着排。拖动时被拖的那个变淡、落点描一圈虚线；按在输入框里照常选字，不会拖起来
- **槽位太多时先提醒**：维度相乘超过 500 个槽位时，加维度前弹框确认（仍然可以加），槽位说明也变成警告色
- **配列的「按音位表填充」**：空着的栏直接填；已经有内容的先问要不要重填（可以撤销）；填完用提示条说清楚各填了几个，和音位表一致时也会说；辅音、元音除了按 IPA 表认，还认名为 C / V 的音类，表里没有的多字母写法（ng、aa）按里面的字母认，普通键盘的 g 也算 IPA 的 ɡ；看不出是辅音还是元音的单独列出来
- **文字的「自动分类」按转写值分**：转写值按主正字法读成音，全是元音的归「元音符号」、全是辅音的归「辅音」、辅音元音都有的归「音节」，转写值是标点、数字的照算；没有转写值的才按字符本身（字母、附标、私用区字形……）；自己起名的分类不动；分完提示各分了多少。从字体或粘贴导入的新字形也按这个分
- **CSV 导入可以不认小括号**：选项里多了「小括号也识别成语域等标记」（默认勾着，跟以前一样）；不勾时序号后面、词类标记后面的小括号一律原样留在释义里，不进标记表；预设会记住这一项
- **改名跟着走**：「词类与维度」里给词干槽、维度取值改名（改完离开输入框时），词库里按旧名存的词干、屈折形挪到新名下，词库的列、文字的「转写来源」、构形流水线里写的词干名一起改，提示条说挪了几个词条；新名下已经有内容的不动。构形换维度先后时屈折形也跟着挪
- **双击直接改**：词库、语素的表格里双击一行，右侧检视器打开并切到录入模式
- **导入 / 导出的图标**：导入换成往里的箭头、导出换成往外的箭头（词库、语素、语料、短语、音变的菜单，文档与词典的导出按钮，文字的导入字体，表格导入的选择文件）
- **规则里的「满足 / 不满足环境」**：目标、替换里写一个 `?`，一条规则就分成两路——`x > a?b / 环境` 满足环境的 x 改成 a、其余的 x 改成 b；`x1?x2 > a?b / 环境` 满足时改 x1、不满足的地方把 x2 改成 b；`x1?x2 > a / 环境` 两路共用 a。a、b 可以是音类对应、`\` 换位（满足时换位写 `\?2`）、`2` 双写或留空删除；两路都按这条规则改之前的词判断、一次改完。右上角规则语法加了「满足 / 不满足环境」一节；链图里这种规则画成两条边，源代码里的 `?` 上色
- **排除可以写几个**：`- 排除 , 排除`，落在任何一个里都不改；规则的表单里「添加排除」可以加好几行
- **规则里的反斜杠转义**：`\?`、`\.`、`\#`、`\C` 这样写就是字面上的那个字符，不当通配、音类或分隔符；单独一个 `\` 仍是换位。规则语法文档补上了转义和按条件换字母
- 示例工程：Aelith 的「Proto → Aelith」改成真能推出词库里现代形式的规则（*kasu → kaso、*nol → nöl、*sor → sör-，原来推出来是 kavo、nol），并用上两路规则 `θ > t?s / #_` 和两个排除的 `u > o / _# - k_ , g_`；Tsahun 的罗马化转 IPA 加了连读变调 `35 > ˧?˧˥ / _C , _V`（叠词 lun35lun35 读 lun˧lun˧˥）
- 示例工程 Aelith：名词加上「性」（sila、vene 是阴性，与格 silaga / venege，其余 kasoka），「动词」绑了变位法一、变位法二（tur-、sal- 用变位法二，过去时 turtim）和不规则三个构形
- 修复：文字的字形表里给问号 `?` 配了字形之后，所有的词都被转写成这个字形；`.` `#` `*` 这类符号、跟音类同名的大写字母当转写值时同样会被当成规则符号
- 修复：文字转写时，句号被当成隔开字母的点号吞掉
- 修复：构形的槽位多了（比如 6 × 6 × 3 = 108 个）以后，往下滚也只显示前 60 个——分批显示的「再加载一批」在页面自己的滚动区里触发不了
- 修复：皮肤页预览里，语料例句的文字行显示成方框（没用那套文字自己的字体）
- 修复：目标里音类前后还有别的字时音类对应不上——`kV > kU`、`[ptk]a > [bdg]a`、`C1V > C1W` 会把那个音删掉；现在目标里第一个音类照样按位置对应
- 修复：自动标音把整段文字当成一个词跑规则，`A B` 里 A 的末尾不算词尾，写了 `_#` 的规则只作用到最后一个词；现在按空白逐词转写，词两头的逗号、引号也不挡词末规则。整库演化同样逐词（跟音变测试台、文字转写一致）
- 修复：构形换维度先后（箭头或拖动）之后，这个构形的写法、变体和停用的槽位全都对不上、显示成「无」（槽位的 key 按维度先后拼）；现在按新顺序重排
- 修复：皮肤页的预览取了语料例句的原文和译文，切分与 gloss 却还是内置示例；现在切分、gloss、译文、文字行都来自同一句，按词上下对齐
- 修复：词条卡和悬浮词卡里，词干、屈折形的名字太长时压在右边的形式上；现在名字一栏按最长的名字定宽、最多占一半多，更长的折行
- 修复：悬浮词卡里指到拆开的某一块时，拆解那一行消失，整张卡跳一下；现在那一行一直在，指着的那一块高亮
- 修复：构形流水线的「增加步骤」菜单，鼠标移开后不会收起；现在移开一会儿就收，按 Esc 也收
- 修复：词条录入里给新加的词干（或屈折形）填上名字后按 Tab，焦点跳不到旁边的输入框，要再按一次；新加的一行有时排到已有那行上面；改成已经有的名字会把那一项盖掉（现在提示重名，名字换回原样）

## 0.8.0 · 2026-09-13

- **长列表分批渲染**：词库、语素、语料、构形槽位、字形表这些长列表改成先画一屏、滚到快见底再画下一批；切页面、改筛选、重新排版都不再卡一下
- **页签与变体改名**：文字、音变、构形的页签，还有构形的变体，鼠标放上去右侧才出现一支铅笔——变体就地改名，页签点了会把检视器里的「名称」聚焦，直接改
- **板块可以收起来**：词类与维度、音系的音位表与元音表、文字的音节拼合与自动映射、皮肤的词条卡与字体库，标题右边有个平时不显示的三角，点一下把整块收起（记在本机偏好里，下次还是收着）
- **词条卡可以调**（皮肤 → 词条卡）：整张卡的字号 80% ~ 160%，每一块（词头与发音、义项、标签、词源、词干与屈折形、关系、派生词、备注）还能单独设倍数；除词头外的几块拖着排顺序，鼠标放上去描一圈虚线
- **发音两边的符号**（设置 → 应用）：宽式 `/…/`、严式 `[…]`，或者什么都不加
- **语法维度按词类限定**（词类与维度）：每个维度可以指定用于哪几个词类；词条录入时只列出对得上的那几个维度
- **清空内容**（设置 → 清空内容）：挑一个模块——词库、语素、语料、短语、文档、规则集、构形、词类、维度、检视器模块、缩写表、文字、配图——整块清掉，方便重新导入；点了先问一次，清完可以 Ctrl+Z 撤销
- **第一次打开软件先讲一遍**：开始页自动走一遍图文引导（新建 / 打开、起步模板、最近打开、底部那排），最后交代右上角的「规则语法」和每页的「使用指南」；所有图文引导换上了新的像素画气泡——气泡是软件这套绿，上沿趴着一只像素猫，里面的按钮跟着改成看得清的配色
- **塞擦音**：音位表补上塞擦音一行（t͡s d͡z t͡ʃ d͡ʒ t͡ɕ d͡ʑ k͡x…），带连音弧的写法与 `ts` 这样的写法算同一个音，特征照样推得出来
- **文字的转写来源**（文字检视器）：这套文字按词条的哪一栏转写——单词（默认）、某个词干、某套正字法的发音，或某个检视器模块；那一栏空着就回落到单词
- **音变规则可以拖**：规则卡片拖到别的阶段，或拖到某一条前面；正字法的规则列表同样能拖，正字法的测试台也按阶段分列显示
- 改名铅笔藏起来时不再占位置，鼠标放上去才让出空档
- 悬浮词卡：切分条与「在词库中查看」跟着词走——词在上面就都挤到卡片顶上，词在下面就都放到卡片底下
- 音变、正字法拖动规则时，落点会空出一行虚线框，松手就插在那儿；落点按指针的高度算，空出来的那一行不会再把判定顶回去来回抖
- 示例工程与瑟乌丝林语夹具的语法维度也按词类限定（名词只看数 / 格 / 名词类别，动词只看式 / 体 / 时 / 焦点……）
- 修复：词库表头的筛选面板挪到最上层，贴着检视器时不再被裁掉右边
- 修复：音节拼合没设消音符时，词尾辅音会凭空多出一个借来的元音（预览里比原词多一截）
- 修复：语料里用 `=` 拆词，显示时会被改回 `-`；现在写什么就是什么（没写的仍按语素类型定）
- 修复：音系测试、文字测试里出现两个相同的词就崩
- 修复：规则环境里的音类名被当成多合字母（`{Vlong}` 里的 ng 变成 N），`@语素` 引用同理；现在音类名与引用原样保留
- 修复：词源的「自定义来源」一行挤成竖条；规则编辑里的音类按钮、皮肤预览里的色块排不下时换行，不再溢出去被挡住
- 修复：语料悬浮卡在页面靠下时贴着上边画，离词很远；现在翻到词的正上方
- 修复：表头筛选面板被检视器盖住
- 修复：规则语法文档里带 `\|` 的表格串行
- 修复：文字页点「新增规则」后，下面的「音节拼合」「自动映射」不跟着往下排
- 致谢名单更新

### 0.7.3 起累积的改动

- **检视器模块**（词库 → 词类与维度 → 最下面）：给词条加一块自己的内容，比如意音文字的异体字、文化注释、地域分布——写标题，选「一段文字」或「几项」，选放在词条卡的哪里（默认在释义与词源之间，也可以在释义上方、词源下方或最下面），可以只给某几门语言用、用某套文字的字体显示。
  录入模式里按同样的位置出现输入框；顶栏搜索可以写 `标题=内容`，词库的「列」里能加成一列；删掉模块时各词条里填的内容一起删，可以撤销。
  CSV 导入时列名跟模块的标题或「导入时也认的列名」一样就自动对上，也可以手动选「检视器模块」、写个新标题，导入时新建（「列映射」旁的「?」有说明）；导出的词条 CSV 每个模块一列，可以直接导回来
- **CSV 导入：词类缩写后面的括号**：`n. (archaic) …`、`adj. (slang, vulgar) …` 这样紧跟在词类缩写后面的圆括号列进「单词、释义、备注里的标记」表，可以设成语域（逗号隔开的算几个语域）或标签；
  括号里像一句说明的（`(placed before the noun)`、`(+ abs.)`）默认原样保留。同一个词类缩写管着的后几个义项开头的括号也算
- **构形的基础变体可以改名**：变体条里的「重命名」一直都在，没选变体时改的是基础那一套（默认叫「通用」，比如改成「书面」）；词条的变体下拉、CSV 导入的构形变体都认新名字
- **默认窗口加宽**：默认宽度改成 1580（屏幕放不下就占满工作区宽度），首页打开右侧面板时底部那排按钮不再挤成好几行；以前记下的窗口比这个窄的，这次打开先放宽一次，之后照常记住拖出来的大小
- **示例工程加深**：Aelith 与 Tsahun 补上检视器模块（刻文异体、异体字用文字的字体显示）、复合词类与义项自己的词类、一个义项几个语域、词干槽、自定义的词源类别与关系种类、改了名的基础变体、
  作用于所有词的连读浊化（语料里反推认出 dovar）、`@词条` 引用与带空格的屈折形（`ngo21 tui55`）、分不出来的同形词候选、故意没进词库的人名（悬浮时「没有找到」）、手填的例句文字写法、文字的括号设置、文档里的 `[[词]]` 链接；首页的示例说明跟着更新
- 修复：音变等规则的源代码视图往下滚之后，超出第一屏（25 行左右）的行号不见了
- 修复：词源类别是自己写的文字时（比如导入的 `bor. from CS`），词条卡、语素卡、关系图里露出 `lexicon.etyTypes.…` 这样的键名；现在原样显示。导入时也认得 `bor.`、`inh.`、`der.`、`comp.` 这类缩写

## 0.7.2 · 2026-09-11

- **一个义项可以有几个语域**：录入模式里语域改成标签式输入（回车添加，可以几个），词条卡并排显示几个方框标签；CSV 导入时连着的几个标记、语域列里用逗号或顿号隔开的几个都设成语域，导出写成 `【古语】【文学】` 连写。
  旧项目的语域打开时自动转过来；存盘时每个义项另带一份拼起来的旧字段，0.7.1 及更早的版本打开也看得到语域
- **检查新版本**：程序开着时每隔几分钟（默认 5 分钟）自动查一次，有新版本立刻在右下角提示；间隔在「设置 → 每隔几分钟检查一次新版本」里调。
  点「稍后再说」之后同一个版本这次不再弹，出了更新的版本照样提示
- **词类标记**：CSV 导入词条时，义项开头像 `n.` `v.` `adj.` `v因.` 这样「拉丁字母 + 几个字 + 点」的写法列成一张「词类标记」表，逐个选：设为词类 / 设为义项标签 / 只去掉 / 原样保留。
  常见缩写已经对好（n. 名词、v. 动词、adj. 形容词……），写已有词类的名字或缩写就用那个词类，`名词/动词` 这样斜杠连着写是复合词类，没有的会新建；
  一个词类标记管到同一格里下一个词类标记为止，词类列里写 `n.`、`n./v.` 也按这张表
- **复合词类**：「词类与维度」里每个词类可以加「组成」，比如「名词/动词」由名词和动词组成（名字、缩写没改过时按组成自动拼）；
  复合词类自己没绑构形、没设词干槽时沿用组成词类的；按词类筛选、`pos=` 搜索、整库演化的词类过滤都把组成算上
- **义项的词类**：录入模式里每个义项可以单独选词类（复合词类的词条先列出它的组成）；跟词条不一样时，词条卡、悬浮卡、词典导出在释义前面用淡色标出。
  CSV 导入时义项分属几个词类的词条自动设成它们组成的复合词类；导出 CSV 时义项的词类写成释义前的 `n.`，可以直接导回来。
  合并同形词条也改成这样：各义项记下原来的词类、词条设成复合词类，不再往释义前面加字
- **导入样例**：所有导入都先在右侧检视器里显示导入后的样子（前几条）——词库 / 语素的 CSV 向导、语料与短语的表格和 JSON、字形的粘贴与从字体读取、Lexicanter 词库、音变规则文件；
  改了任何设置样例马上跟着变，变了的地方先高亮再慢慢褪掉。
  语料、短语的表格导入从弹窗改成页面主区里的面板（好让检视器露出来），从 JSON 导入也走这个面板；从字体读字形、导入 Lexicanter、导入规则文件都先看样例再点「导入」，规则文件可以在面板里改格式和规则集名
- **导入能填到录入页面上的每一处**：CSV 导入词条新增语言、方言（没有的新建）、词源类别、词源来源（可填来源语言，是项目里的语言时直接链到同形的单词或语素，`a + b ‘意义’` 拆成几个来源）、词源中间态、关系（选种类，按单词名找目标，这次导入的其他行也算）、各正字法的发音、文字写法、构形与变体、带前缀的备注；
  导入语素新增第二形式、异体形（`lar / _V；ler / Front_`）和词源各项；字段下拉按「导入为」只列能填的。
  语料的表格导入新增别的正字法写法、文字写法、自由行（列名当标签），短语新增各正字法的发音、带备注的变体（`说法（备注）`）
- **释义开头的编码自动识别**：「1离开；2前往」这类紧挨着释义的数字识别到了才显示一张表，逐个选设为义项标签 / 只去掉 / 原样保留（填了标签就是设为标签），不用再手写「编码=标签」，老预设里的写法自动转过来；「2020年」这种长数字不算
- **整个项目导出为一个 CSV**：「设置 → 导出」里导出，一段一张表（项目信息、语言与音位、文字与字形、规则集、词类、语素、词条与义项、构形、例句、短语、文档……），嵌套的内容拆成子表，内嵌字体、配图这类超长内容分段存；
  在表格软件里改完，用「打开项目」选这个 .csv 就能原样读回来（读回来是未保存的项目）
- **提示语精简**：去掉导入样例上方这类可有可无的说明，长说明收进标题旁的「?」，各页顶部的提示条改成一句话
- **统计面板**：并排的两块按矮的那块对齐，长的那块在里面滚动；每块加一圈淡淡的圆边框，鼠标放上去那块略微亮起
- 修复：CSV 导入时词类缩写后面紧跟方括号标记（`n.[中古][文学]金石`），会切出一个只剩 `n.` 的义项
- 修复：义项的词类下拉框里「词类跟词条」这类长文字盖住了下拉箭头
- 修复：语料悬浮卡顶部的切分里，有 gloss 却挂不上词条的那一段（如 `Iádhon·wéñgaus` 里的 `wéñgaus`）点了没反应。
  现在形式对不上时再按意思找——释义对得上这段的 gloss、某个形式出现在这一段里就算（`wéñgaus` 里有 `eñgos` 的弱焦点形 `eñgaus`）；
  仍然挂不上的段一律标成「没有找到」，点开手动指定；悬浮时整个词对应的词条也跳过已经挂着语素的段、取词干那一段
  （`Iádhon·wéñgaus` 显示「降临」而不是体标记撞上的「完成」）

## 0.7.1 · 2026-09-11

- **页面状态都记着**：词库、语素的表头筛选与排序，各页的子页、选中项、测试台与预览里写的字……离开页面再回来（点导航或按「返回」）都还在；换了当前语言时不恢复选中与滚动位置
- 左侧的模块按钮再点一次停在当前模块，不再跳回上一页；皮肤、设置仍是再点一次回到上一页
- **查语法挪到顶栏**：检视器按钮与关闭项目之间的翻开的书图标，在音变、构形、文字、音系、语素页打开时直接滚到相关的那一节
- 搜索框的「?」紧贴在搜索框后面
- **词条卡**：义项的语域显示成方框标签（中文一个字，英文缩写如 `lit.`；「设置 → 词条卡里语域的显示」可改成全称）；例句与词条卡之间换成分割线，整体间距放宽
- **语料里分不出来的词也弹悬浮卡**：整个词找不到、或拆出来的某一段找不到时，卡片写明「没有找到」，在搜索框里按写法、释义或 gloss 挑一个词条或语素指定给它；每一段都认出来后记为已确认
- **CSV 格式说明**：使用指南补上词条、语素、语料、短语的表格列名、写法与示例，导入向导和表格导入对话框里有「格式说明」直达；
  导出的词条 CSV 义项改用分号分隔、语域写成义项前的【语域】，语素 CSV 的 `meaning_zh` 这类列名导入时自动认出——导出的表格可以直接导回来
- 修复：构形里 `@引用` 在语素表里找不到时，后面写的空格被吃掉；现在照样保留，并且会找同名词条（`@mo` 引用词库里的 mo）
- 修复：语素异体形的环境里，音类按 IPA 写、词干是拼写时对不上（`C` 里是 `k`、拼写是 `c`，`_CC` 认不出 `cl-`）；现在拼写和按主正字法转出来的读音各比一次

## 0.7.0 · 2026-09-11

- **自定义文字换上自己的字体**：导入或指定了字体后，字形表、检视器标题、「字符」输入框都用这套文字的字体显示，不再是方框；
  皮肤页的字体槽下方列出当前项目里的每套文字，可以单独指定字体（字体库里装的、本地导入的都行），覆盖文字自带的设置
- **图文引导**：点各模块标题旁的「使用指南」，先在界面上一步步圈出要点——高亮、箭头加说明，（1/N）往下翻；
  讲完弹窗问要不要去网站看完整教程。每个模块默认只自动讲一次；弹窗里的勾选项（默认关）或「设置 → 点使用指南时每次都先看图文引导」打开后每次都先讲
- **语料悬浮挑候选**：一个词对得上几个同形词条时，先看这句自己的译文，再参考别的例句——同一个词在别处确认成了哪条、
  出现它的句子译文里提到了哪条——自动挑出来；仍然分不出来就在原文下画警告色波浪线，悬浮时把候选并排列出（最多四个），
  点「就是这个」固定下来（记为已确认）
- **书签页签**：文字、音变、构形里自建的多套内容改成书签样式，选中的页签与下方页面连成一片、两侧弧线过渡；
  文字页的头部与其他模块对齐——页签在左，「新建文字」在最右
- **文字的括号设置**（文字检视器）：原文里的括号怎么写成文字——括号照留、里外分开转写（默认）/ 去掉括号、内容并进词里 / 括号连内容都不写。
  修复：此前括号被当成词的一部分，紧挨括号的词首、词尾规则失效，文字行写错
- **词干槽可以配置了**（词库 → 词类与维度）：每个词类声明自己有哪几个词干（如强形 / 中形 / 弱形）并写上说明；
  词条录入模式按这里逐个列出，构形流水线的「词干」下拉也从这里给候选，旁边的提示写明词干从哪来
- **模糊匹配时忽略的字符**（语言检视器）：关系图跨语言找词、词源来源搜索时先去掉这些字符再比，
  比如喉音 H1 / H2 / H3 都算 H、词根里分音节的点；变音符与大小写一直是忽略的
- **CSV 导入：义项前缀映射**：释义开头的编码（如动词价态「1离开；2前往」）从释义里拿掉、变成义项标签（「一价」「二价」），
  每行一条「编码=标签」，编码可以连写；随列映射预设一起保存
- 测试夹具（瑟乌丝林语）的动词头改由构形推导：语素表只留不可再分的感音、式、体、时、否定等语素，
  语料里的动词头拆成这些语素，悬浮时逐段可看；语法书拆解表的 9 个例子全部对上。上面几项通用设置就是从这份数据里提炼出来的

- **首页致谢**：首页底部新增「致谢」，列出像素画创作、赞助、测试和意见反馈与其他支持的朋友
- 修复：语料「全部确认」后编辑器渐隐完又冒出来；现在收起并保持收起，检视器仍停在这句上，再点一下卡片才展开
- **顶栏高级搜索**：`字段=内容` 只在一个字段里找（如 `gloss=PL`、`word=kam`，也认「释义=」这类中文字段名），
  `字段==内容` 要整个相等，`/正则/` 按正则找；多个条件用空格隔开，内容带空格就加引号。搜索框旁的「?」列出本页的字段
- **「返回」回到原来的位置**：历史里记下当时的当前语言、选中的对象与子页——关系图里点了别的语言的词、词条卡里点词、
  语料里点词跳到词库，按返回都回到原来那门语言的那一条；一致性检查跳到文字页时也会选中那套文字
- **作用于所有词的构形**（构形检视器里的开关）：词首音变、连读变化这类不用绑定词类，也不往词条里写屈折形；
  语料分词时拿它反推——从词库里挑词喂给这条构形，归纳出「词首 X 变成 Y」的对照表，把表层词换回原形再查
  （na-wener → mener「天穹.LEN」），剥过前缀的余部也会反推（e-wenallan → menallan）
- **分词更泛用**：撇号缩略按省掉的元音补回去找（t'am、m'nem，要先把 ' 写进语素边界符号）；写了重音或附加符的词去掉附加符再找、实在找不到时试着拆成两个词（这两种算猜测，确认前仍标黄）；
  按边界切开的每一段也会剥一层词缀、反推词首音变；悬浮时对应的词条取最长的那段（介词缩略 + 名词时是名词）
- 修复：构形里 `@语素` 引用后面写的空格、中点被吃掉——`@定指·` 现在推出 `sa·derg`，写在前面的也照样保留
- 文字页的「字形表 / 映射规则 / 预览」挪到标题左边，与其他模块一致
- 字符面板「项目字符」里的字形、语料导出预览里的文字行都换成这套文字的字体（导出预览的拉丁部分仍按等宽对齐）
- **导入导出补齐**：语素页从 CSV 导入、导出 CSV；音变页导出当前规则集或全部规则集（每个一份 .txt）；
  语料与短语从表格导入（CSV / TSV / 一行一条的文本，逐列挑字段，原文重复的跳过）、导入导出 JSON（语料连分析一起），
  语料还能把当前列表导出为莱比锡 / Markdown / HTML / LaTeX
- 测试夹具（瑟乌丝林语）加了词首音变构形：软音变、流音音变按语法书例句写了对照，鼻音、闭锁、混合音变待补

- **规则语法统一**：音变、正字法、文字映射、构形的微调、语素异体形的环境都按同一套规则写法。新增**编号音类**：
  同一条规则里编号相同就是同一个音——`_CC` 是任意两个辅音，`_C1C1` 是同一个辅音双写，`C1C1 > C1` 把双写辅音变单写；
  替换里的临时音类（`[ptk] > [bdg]`）也按位置对应了。首页新增「规则语法」，音变、构形、文字、音系、语素页标题旁的「查语法」在右侧检视器里打开完整说明
- **「返回」记住操作**：一步步退回走过的位置——页面、当时的当前语言、选中的那一条、打开的子页或窗口（统计、关系图、录入模式、字形表……）、
  列表滚到哪、搜索框里写的字；在同一页里换了词条、切了子页也各算一步，指向已删除对象的步骤自动跳过
- **带空格的屈折形**：词典里写成两个词的形式（`ar mae` 这种限定词 + 名词）会把原文里连着的词并成一个词再分析；确认过的词不并
- 按边界符切开的词（`la-hethí`、`na-wefer`）每段留几个不同词条的候选，拼出几种分析放进候选下拉，第一种挑错了还有得选；
  反推出的词首音变会正着推回来核对，对照表丢了上下文时（`t > d` 碰上 `th`）不再给出错的原形；词首音变对照表先按正字法的多合字母统一写法再比较
- 词首音变这类「作用于所有词」的构形可以限定只作用于某门语言
- 语料、短语的表格导入新增「分列」：默认自动判断，不是每行都有同一个分隔符时当成一行一条（句子里的逗号不再把句子切开）；导入后在新句子里自动查重
- **CSV 导入：方括号标记映射成语域**：单词、释义、备注里出现【专】【神】〔古〕[arch.] 这类标记、或者序号（`1、` `2.` `3)`）后面紧跟的第一组括号（圆括号也算）时，
  向导列出每种标记的次数、所在列与例子，逐个选设为语域（默认，名字可改、可从常用语域里挑）、设为义项标签、只去掉标记或原样保留；
  释义里的标记管到下一个标记或分号为止、前后拆成不同义项，备注里以标记开头的一段变成新义项，单词前的标记管整个词条；随列映射预设一起保存。
  词库搜索新增 `register=`（语域）
- 测试夹具（瑟乌丝林语）的词汇按词表里的标记设了语域：人名、专有名词、地名、星名、神祇、文学、引申、古语；备注里的人名另起义项，短语记成标签
- 修复（代码审查）：`@语素` 写错或不存在时不再悄悄对上更短的引用，照样提示「未找到语素」；文字的括号「并入 / 省略」贴着词时不再把词切开，
  `[占位符]` 里的括号不转写，词两头的标点单独转写（词末规则不再被逗号挡住）；CSV 义项前缀：字母编码后面还是字母时不算编码，`1、离开` 先认编码再去编号；
  JSON 导入逐项清洗，不合规的文件不会弄坏语料页；词干槽输入第一个字后不再丢焦点、重名不再崩；点有几个候选的词先挑候选；
  `script=` 搜的是列里显示的转写文字；讲解途中换页面就结束讲解；帮助气泡里的换行照样换行；模糊匹配不再把 kʷ 当成 kw；导出全部规则集时只差大小写的文件名不再互相覆盖

## 0.6.8 · 2026-09-10

- 修复：**环缀作为词源来源时，显示模式只剩前半**（`e…ce` 变成 `e`）；词条卡、词库的「来源」列、关系图现在都写成「前半…后半」
- 构形的词缀里**写进去的空格与中点不再被吃掉**：前缀写「ė 」推出 `ė derg`，写「an·」推出 `an·derg`（整格全是空白仍当没写）
- 修复：词条卡的「派生词」标题漏了文案键，界面上直接印出 `lexicon.derivedWordsWords`
- 新增一组**文案键自检测试**：界面里用到的每个 `t('…')` 键都必须在中英两套里存在、两套结构一致，
  模板键（`t(\`x.${y}\`)`）的前缀也要是两边都有的对象——同类问题以后跑测试就能拦住

## 0.6.7 · 2026-09-10

- **顶栏搜索覆盖所有模块**：语言、音系、文字、音变、语素、词库、构形、语料、短语、文档、皮肤、设置各有自己的占位文字与筛选口径
  （音位与音类、字形、规则行、槽位、文档正文、字体、设置项……）
- 词库与语素的**自定义顺序按钮挪到表头最左侧**，与上下移动按钮同一列，列宽再窄也找得到
- 文档页打开默认停在**预览**（新建页面仍直接进编辑）
- 新增**分词方式**设置（设置 → 项目）：按空白（默认）/ 逐字（汉语式、日语式这种不用空格的表记）/ 自定义分隔符正则
- 字体库：加入随软件一起带的 **gilatod unicode**（【荏苒之境】百科标准字体，点「安装」即可，不用联网），
  另补 14 款可免费商用的西文字体——Andika、Cardo、Inter、Noto Sans、Fira Sans、IBM Plex Sans、EB Garamond、
  Libre Baskerville、Lora、Source Serif 4、Merriweather、Fira Code、Noto Sans Mono、Noto Sans Old Turkic
- 示例项目 Aelith 的刻文去掉了每个字母之间的分隔点，并补上 z 的卢恩字母

## 0.6.6 · 2026-09-10

- 修复 0.6.5 的回归：词库在选中某个词之前会多出一列「语言」；现在只有顶栏切到「全部语言」时才出现

## 0.6.5 · 2026-09-10

- 悬浮词卡的**成分条挪到卡片顶部**；从词卡「在语素中查看」跳过去后定位到那一条并高亮渐隐
- 语料「全部确认」后检视器保持在这句上，不再清空
- 词库与语素的列表改成**表头操作**：点列名循环 ▼降序 → ▲升序 → 取消；列名右侧漏斗打开筛选面板
  （全选 / 全不选 / 单选，第一列按首字母）；去掉左上的词类 / 标签 / 排序下拉，自定义顺序变成表头最右侧的小按钮；
  筛空了表头仍在，随时能取消
- 各模块的**搜索框统一挪到顶栏**，就在项目名与保存状态右边，占位文字跟着当前模块走（词库、语素、语料、短语）；换页面自动清空，不再占用每个模块自己的版面
- 关系图：自定义来源与中间态里拼接的形式（`gēs-sal`、`a + b`、`x·y`）拆成多个节点，
  每个成分到来源语言里找同形的词条或语素，找到的能点过去（跨语言跳转）
- 语料**查重合并**：原文相同、只差出处的直接合并，出处写成「A & B」；相似度 ≥80% 但不完全一样的弹窗让你勾选。
  瑟乌丝林语示例跑过一遍：合掉 19 对，剩下两对是真不同的句子
- 构形测试台：勾选了词类只列该词类的词；点进输入框就按字母序列出候选
- 文字新增**竖排显示**开关（默认关）：勾上后语料、短语、词库、词条卡里的这种文字都竖着排，列的走向跟书写方向
- 检查更新：点「下载并安装」后**应用内下载**安装包（带进度），下好先保存项目，Windows 上按上次的安装选项静默安装并自动重开；
  macOS 打开下载好的 dmg
- 修复 `e·(ñ)galan` 这类带括号的转写：括号不再被当成辅音拼进音节
- **统计面板**：词库、语素、语料各一份（数字卡 + 分布柱 + 排行，点分布项回列表筛选）；
  构形的一致性检查下方新增**项目一致性**：断掉的引用、漏填字段、重复条目、没接上的配置、缩写表缺的缩写、
  字母表之外的字符，按严重程度分组，点条目跳过去
- 皮肤预览：十五项颜色与七个字体槽在预览里各有对应样例；左边改哪一项右边对应处闪一下，悬停时描边提示
- **示例工程重做**：Aelith（黏着语）与 Tsahun（孤立声调语）两个虚构项目现在把每个模块的功能都摆了一遍——
  语系与祖语词根、元音和谐、流水线构形的八种步骤、变体与继承、屏蔽槽位与手填表、词源链与关系图、配图与方言、
  已 gloss 并确认的语料、短语占位符、文档、缩写表与导出模板；Tsahun 另有声调、双正字法、音节文字拼合与竖排。
  首页新增「示例工程」按钮，一点就打开（改动不会覆盖示例文件本身）
- 从词源来源自动建的关系（复合、派生、音变…）显示时也用词源类别的名字，不再露出英文键名
- 修复：窗口被挡住或最小化时，整库演化、批量推导、一致性检查这类分批跑的活会被系统的定时器节流拖到几乎停住；
  现在关掉了后台节流，切走也照常跑完

## 0.6.4 · 2026-09-10

- 词库列宽修好了：此前固定布局配 `width:max-content`，浏览器会退回按内容分配，
  于是拖左边几列没反应、标签列乱跳；现在表格宽度按各列之和给出，逐列拖动都精确生效
- 首页底部的更新日志 / 开发者 / 友链 / 赞赏改到右侧面板显示，与检视器同一套样式
- 补齐 0.5.0 起的更新日志
- 语料切分：先扫一遍例句登记动词头再建 token；语法书把 gloss 压成一个词时也按中点拆开；
  人称不在表里（如「三双」）时退到同语流的其他前缀；索引与查询各加一份去附加符的写法；
  查不到就剥一个语素前后缀、再逐段试，但仍要求释义与标注对得上

## 0.6.3 · 2026-09-09

- 构形的「流水线」措辞简化为 流水线 / 音变 / 增加步骤；微调那一步可展开成多行框
- 悬浮词卡点过组成部分后**钉住**，不再因为卡片变矮而消失；有关闭按钮，点别处或 Esc 收起
- 悬浮卡底部按钮按当前显示的是词条还是语素切换文案与跳转目标
- 词条卡的屈折形改成整宽表格，行距化开、隔行底色；留空的词干与屈折形不再占位
- 词源的来源改成**可搜索的下拉**：环缀显示成「前半…后半」，形式、gloss、意义、类型名都能搜
- 音变环境里 `{@语素}` 不再报「未定义的音类」，示例词也能生成
- 词库列宽：拖动一列时先把所有列的当前宽度定下来，表格按内容宽度走
- 首页底部的更新日志 / 开发者 / 友链等改到右侧面板显示，与检视器同一套样式
- 瑟乌丝林语示例：名词与形容词的屈折形按语法书写成「词头·词干」（có·ubs）；限定词建成语素

## 0.6.2 · 2026-09-09

- **构形生成器改成流水线**：起点只有词干，前缀、后缀、中缀、**环缀**、模板、重叠、音变、微调都是可排序的步骤，
  可以出现多次、任意排序；旧项目的四种生成器载入时自动转成等价流水线，推导结果不变
- 悬浮匹配加「意思要对得上」的门槛：同形候选按标注与释义的片段重合挑，一个都对不上就不给卡片
- 短语里 `[名词]` 这类方括号占位符不再参与文字转写
- 规则示例词认得 `{@语素}` 音类；词条卡不再列出空的屈折形
- 新增**启动时检查新版本**：有更新在右下角提示，可跳过某个版本或在设置里关掉；只提示与打开下载页，不动工程文件

## 0.6.1 · 2026-09-09

- 分段按钮（显示 / 录入、变体切换、列表 / 源代码…）的样式收进全局，各页面统一
- 语料的确认面板从列表最上方挪到**被选句子的正上方**
- 悬浮解析优先用**用户确认过的分析**：本词确认过就直接用，同一写法在别处确认过也照搬；
  卡片底部按确认的切分列出每一段，能对上语素或词条的可以点开
- 修复「查看全部例句」在同一出处重复命中时崩溃

## 0.6.0 · 2026-09-09

- 文字模块新增**音节拼合**（CV / VC 音节文字）：拼格直接从字形读音推出来，再配消音符、清浊标记、
  元音份数几项设置，就能表达「短元音写两次、清音跨两格、词首清音配消音符」这类写法
- 提示点「?」气泡改成视口浮层，不再被检视器裁掉
- 耗时操作（批量推导、一致性检查）给进度条
- 语素页加显示 / 录入两种模式，标签列不再破坏表格对齐
- 跳到语料或短语时定位到那一条并短暂高亮；例句弹窗可搜索
- 词库搜索覆盖词干、屈折形、发音、文字、词源等全部文本
- 构形测试台改成模糊搜索整本词库；关系图内点节点就地换中心，跨语言会切当前语言
- 记住窗口大小与位置

## 0.5.1 · 2026-09-09

- 词库列宽可拖动并记忆；同名列会给出提醒
- 词条下方挂载例句（语料 / 短语 / 文档），默认 3 条，可展开全部并滚动加载
- 重复词条可高亮（设置可关）并提供合并，义项顺序拼接、词类叠加显示
- Ctrl / Shift 多选，可批量加标签或删除；文字页字形同样支持
- 语域给常用预设；CSV 导入按中英文分号拆义项

## 0.5.0 · 2026-09-09

- **词源重构**：去掉原始形，改成「来源 > 中间态… > 词条」的链条，类别可自定义，
  来源按类别给语素 / 词条 / 自定义选择器，词条来源自动进关系图；语素也带词源
- 中文术语调整：范式 → 构形、短语簿 → 短语、对账 → 一致性检查、词头 → 单词
- 一致性检查按钮移到构形模块标题旁
- 各模块面板旁加「?」小标记，悬浮给一句用法说明
- 纯欣赏模式：可导出带只读标记的项目，打开后不保存、不给编辑入口

## 0.4.0 · 2026-09-09

### 瑟乌丝林语示例项目

- 新脚本 `npm run examples:theusrin`：从站点仓库的语法书（`laim/Theusṛin.md`）、音变器规则（`SCA/*.txt`）、
  科飒尔文字体（`assets/fonts/kessar.woff2`）与作者词表重建整个项目
- 5 门语言（原始希克林语 → 原始 / 上古 / 古瑟乌丝林语 → 标准语）、36 个音位与双向正字法规则、
  音节与重音、配列表
- 科飒尔文：130 个字形（本征音 / 二分音 / 特征音 / 数字 / 标点 / 功能符），字体内嵌进项目文件，
  一个字形可带多个读音
- 音变：六阶段 231 条规则，阶段全部绑定到对应语言
- 名词变格范式＝真词干 + 格缀 + 整条音变链，可与词典实录逐格对账
- 语料：语法书里的 98 条行间标注例句，带科飒尔文原文、拼写分解与逐词 gloss
- 短语簿 79 条、文档 13 页（语法书全文）、行间标注缩写 42 条、语素 1345 条、词条 1467 条

### 其他

- 例句支持手工指定自定义文字写法（检视器里的「文字写法」），导出与列表都会用它
- 字形的转写值可以写多个读音，用 / 分隔

## 0.3.1 · 2026-09-09 · 试用反馈

- 译文、释义、维度名等多语言文本改为**界面语言优先**显示（英文界面下短语簿、语料译文显示英文）
- 下拉菜单统一改为**点击展开、点空白处或 Esc 关闭**，并自动避免伸出窗口（词库「列 / 导入 / 导出」、音变「导入」）；不再悬停激活
- **撤销 / 重做**：任何项目改动都可 Ctrl+Z / Ctrl+Y（焦点在输入框里时先由输入框自己撤销），顶栏有撤销 / 重做按钮；**返回上一页**按钮与 Alt+←
- **右键菜单**：剪切 / 复制 / 粘贴 / 全选、撤销 / 重做 / 返回、命令面板 Ctrl+K、字符面板 Ctrl+I、保存、使用指南
- **排序**：语素可按字母表 / 类型 / 自定义顺序（上下移动）；词库新增按词类、自定义顺序；词类与维度、维度取值可上下移动或一键按字母排序
- **词条配图**：录入模式添加图片，统一到「设置 → 词条配图尺寸」（默认 320×240）；尺寸相符原样保存，比例相同等比缩放，其余弹出裁剪框；PNG / GIF 无损，JPEG / WebP 沿用原格式；词条卡在词头右侧显示首图，其余缩略图
- 语料自动分析：匹配忽略大小写与 Unicode 规范化差异，找不到时退到祖语链上的语素表（词根在祖语时也能识别）
- 短语簿的词也支持悬浮词卡与跳转词库
- 音变规则可用 `@语素`（gloss 或去掉连字符的形式）引用语素的各异体形，用于辅音交替等
- 范式「微调」列与参数列对齐；只横向溢出的表格 / 链图用普通滚轮即可横向滚动；链图的滚动条固定在可视区底部
- 英文界面侧边栏标签不再贴边；「使用指南」按钮统一放在各页标题旁，首页左栏也有；音变页去掉「语法说明」

## 0.3.0 · 2026-09-08 · M6 打磨

### 短语簿（新板块）

- 分类 → 短语；译文、按正字法规则自动标音、变体（写法 + 语域说明）、标签；带文字行；双击分类可重命名

### 文档（新板块，替代占位页）

- 项目内 Markdown 页面：编辑 / 分栏 / 预览，工具条插入语法；支持标题、列表、引用、代码块、表格、粗斜体、链接
- `[[词头]]` 链到词库词条，预览里点击跳转；页面可归属某门语言；导出 Markdown / PDF

### 整库演化（音变页「整库演化」按钮）

- 选源 / 目标语言、起止阶段、输入字段（词头 / 词干槽 / 源语言语素表）、词类筛选
- 先预览全部推导结果（新建 / 更新 / 未变 / 跳过），再写入目标语言；新词自动带「继承」词源链接，改规则后重跑只更新词头不重复建词

### 词典导出（词库「导出」菜单）

- HTML（双栏打印样式）、Markdown、PDF（桌面版直接出文件，网页版走打印）；可选词干与屈折形、词源、文字、备注、按首字母分组、义项语言
- 逐条模板：`{{lemma}} {{ipa}} {{pos}} {{script}} {{definition}} …` 与 `{{#senses}}` `{{#forms}}` 块，模板可保存到项目

### Ctrl+K 命令面板

- 跨页面搜索词条、语素、例句、短语、文档、规则集、范式、语言，回车跳到对应页面并选中；也可直接输入页面名导航或执行保存等命令

### macOS 与发布流程

- macOS 构建配置（dmg + zip，Intel 与 Apple Silicon，不签名），应用菜单（Cmd+C/V/Z、文件、视图、帮助→使用指南），`sh dev.sh`
- GitHub Actions：推 `v*` tag 自动在 Windows 与 macOS 打包并发布 Release；`npm run release` 一键打 tag；每次 push 跑 lint / typecheck / 测试

### 使用指南

- 每个页面右下角有「使用指南」链接，首页底部也有；音变页「语法说明」改指向指南的规则语言章节。指南托管在作者网站 https://kinnuch.github.io/cerf/qonlang/ ，按模块分 14 页

### 代码审查后的修复

- Electron 不支持 `window.prompt`：皮肤「存为我的预设 / 重命名」、文字自定义分类、短语簿重命名分类改用应用内输入框
- `npm run build` 的类型检查在整库演化模块报错（输入字段联合类型），已修
- 关闭项目回到首页时缺 import 导致整页失灵，已修并给各页加错误边界
- ESLint 现在能解析 `.svelte.ts`，`npm run lint` 零错误；全库按 prettier 统一格式

### 其他

- 新应用图标（青绿圆角方块 + 楷体「千」，霞鹜文楷 OFL）；`build/icon.ico` 供安装包
- 首页开发者信息加 Bilibili 链接；词源类型新增「继承（音变）」

## 0.2.0 · 2026-09-08

### 文字（新板块，位于音系与音变之间）

- 每门语言可定义多套书写系统：类型（全音素 / 辅音音素 / 元音附标 / 音节 / 语素 / 特征 / 混合）、书写方向、字体
- 从 TTF / OTF 字体文件读出字形表（cmap + post 字形名），字体可内嵌进项目文件；也可粘贴字符表
- 字形自动分类、转写值、备注；`@glyphs` 自动映射 + 自定义规则（与音变同一规则语言）
- 词库多出「文字」列与手工覆盖，词条卡、语料编辑器 / 列表 / 导出（莱比锡、Markdown、HTML、LaTeX、模板 `{{script}}`）都带文字行
- 字符面板「项目字符」页列出当前语言的字形

### 皮肤（导航栏左下 T 恤图标）

- 预设：默认、中国古代、竹林、西幻、海蓝、墨夜；浅色 / 深色分别配色，逐项取色器可改
- 字体分槽：界面、语言数据、等宽、语料原文、译文、gloss、自定义文字
- 字体库：内置 OFL 可免费商用字体目录（霞鹜文楷、思源宋黑、Charis SIL、Gentium Plus、Cinzel、Noto Sans Runic 等）一键下载到应用数据目录，或从本地添加；可设镜像前缀

### 语料与联动

- 「全部确认」后编辑器渐隐，例句卡片长出 gloss 行并闪绿
- 悬停例句中的词显示词条卡，可跳到词库定位该词
- 各页顶部有可关闭的联动提示；推导、导入等动作有完成反馈动画
- 首页横幅链接到 Gilatod Wiki；底部：请我喝咖啡、更新日志、开发者、友链
- 英文名改为 Qonlang，可执行文件为 `Qonlang.exe`

## 0.1.0 · 2026-09-08

### M5 语料与自动 gloss

- 例句录入、行间编辑器（切分 / gloss / 候选 / 确认 / 就地建词）
- 自动分析：已确认优先、词头 / 词干 / 屈折形 / 语素、多层词缀剥离
- 导出：莱比锡纯文本、Markdown、HTML、LaTeX、自定义模板
- 语料统计与缩写表（一键填入莱比锡标准缩写）

### M4 形态范式

- 维度笛卡尔积生成槽位；拼接、拼接后跑音变、模板交替、重叠、查表五种生成器
- `@语素` 引用按环境挑选异体形；范式继承；每槽位「微调」
- 测试台、推导写入、与已录入形式对账

### M3 音系

- IPA 表点选建音位表，特征自动推断；音类按成员或特征生成
- 正字法双向规则与自动标音；音节划分、重音、声调表；配列检查与随机造词

### M2 语素与词库

- 语素表、词库（显示 / 录入模式、自定义列、关系图）、词类与维度
- CSV 向导（含粘贴、预设）、Lexicanter 导入

### M1 音变

- 兼容音变姬语法的规则引擎，阶段快照与逐条轨迹
- 规则列表 / 链图 / 源代码三种视图，测试台

### M0 脚手架

- Electron + Svelte 5，单文件项目、自动备份、中英双语、浅色 / 深色
- 全局 IPA 字符面板（Ctrl+I）
